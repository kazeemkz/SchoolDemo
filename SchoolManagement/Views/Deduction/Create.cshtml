@model SchoolManagement.Model.Deduction

@{
    ViewBag.Title = "Create";
}

<br />
<h4>CREATE DEDUCTION</h4>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Deduction</legend>

        <div class="editor-label">
           <b>Amount(NGN) Per Month</b>
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Amount)
            @Html.ValidationMessageFor(model => model.Amount)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.DeductionDescription)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DeductionDescription)
            @Html.ValidationMessageFor(model => model.DeductionDescription)
        </div>

        <p>
            <input type="submit" value="Create" class="btn-primary" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
