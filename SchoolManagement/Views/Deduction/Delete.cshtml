@model SchoolManagement.Model.Deduction

@{
    ViewBag.Title = "Delete";
}

<h4>DELETE DEDUCTION</h4>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Deduction</legend>

    @*<div class="display-label">
         @Html.DisplayNameFor(model => model.Amount)
    </div>*@
    <div class="display-field">
      <b>Amount(NGN):</b>  @Html.DisplayFor(model => model.Amount)
    </div>

    @*<div class="display-label">
         @Html.DisplayNameFor(model => model.DeductionDescription)
    </div>*@
    <div class="display-field">
     <b>Deduction Description:</b>   @Html.DisplayFor(model => model.DeductionDescription)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Delete" class="btn-danger" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
